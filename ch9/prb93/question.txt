The 'grade' function was written to throw an exception if a user tried to 
calculate a grade for a "Student_info" object whose values had not yet been read.
Users who care are expected to catch this exception. Write a program that triggers
the exception but does not catch it. Write a program that catches the exception.

